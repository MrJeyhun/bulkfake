export enum CounterType {
  ERRORCOUNTER = "ERRORCOUNTER",
  SEEDCOUNTER = "SEEDCOUNTER",
}

export enum Regions {
  US = "USA",
  GE = "Germany",
  PL = "Poland",
}

export enum RegionCode {
  USA = "en_US",
  GERMANY = "de",
  POLAND = "pl",
}
